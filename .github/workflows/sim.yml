name: Run KuCoin Bot Live

on:
  workflow_dispatch:    # Permite botón "Run workflow" manual
  schedule:
    - cron: "*/10 * * * *"  # Opcional: cada 10 minutos

jobs:
  run-bot:
    runs-on: ubuntu-latest
    timeout-minutes: 120  # Tiempo máximo de ejecución
    steps:
      # 1️⃣ Clonar repositorio
      - name: Checkout repository
        uses: actions/checkout@v4

      # 2️⃣ Configurar Python
      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: "pip"

      # 3️⃣ Instalar dependencias
      - name: Install dependencies
        run: pip install -r requirements.txt

      # 4️⃣ Crear carpeta de reportes si no existe
      - name: Ensure reports folder
        run: mkdir -p reports

      # 5️⃣ Ejecutar bot en foreground
      - name: Run bot.py
        run: python bot.py

      # 6️⃣ Subir reportes generados como artifact
      - name: Upload reports
        uses: actions/upload-artifact@v4
        with:
          name: kucoin-bot-reports
          path: reports/
